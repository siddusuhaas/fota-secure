# packager

Python CLI tool, runs on a build/release host — produces signed +
encrypted update packages. See `docs/ARCHITECTURE.md`'s `packager` section
for module layout and flow. Built out in Phase 1 of `docs/TASKS.md`.
