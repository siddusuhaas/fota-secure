# consumer

C binary, runs on the embedded device — verifies, decrypts, and installs
update packages produced by `packager/`. See `docs/ARCHITECTURE.md`'s
`consumer` section for module layout and flow. Built out in Phase 2 of
`docs/TASKS.md`.
